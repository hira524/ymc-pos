{
  "name": "ymc-pos",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "heroku-postbuild": "cd backend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["pos", "inventory", "stripe", "gohighlevel"],
  "author": "",
  "license": "ISC",
  "description": "YMC Point of Sale system with inventory management",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "dependencies": {
    "axios": "^1.12.0",
    "dotenv": "^17.2.1",
    "qs": "^6.14.0"
  }
}
