# Frontend Environment Variables for Production Deployment

# Backend URL (Replace with your Heroku app URL)
# This is a public URL and not a secret - it's bundled into the React build
REACT_APP_BACKEND_URL=https://ymc-pos-backend-a6db766d7240.herokuapp.com

# Stripe Location ID for physical card readers (Required for live mode)
# Get this from your Stripe Dashboard > Terminal > Locations
REACT_APP_STRIPE_LOCATION_ID=tml_your_location_id_here

# Optional: Force test/live mode (overrides backend detection)
# REACT_APP_FORCE_TEST_MODE=true
