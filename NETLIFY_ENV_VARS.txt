# 🌐 NETLIFY ENVIRONMENT VARIABLES
# Add these in Netlify dashboard: Site Settings > Environment Variables

# Backend URL (replace with your actual Render URL)
REACT_APP_BACKEND_URL=https://your-app-name.onrender.com

# Stripe Location ID (get from Stripe Dashboard > Terminal > Locations)
# ⚠️  IMPORTANT: You need to create a location in Stripe Dashboard first!
REACT_APP_STRIPE_LOCATION_ID=tml_YOUR_STRIPE_LOCATION_ID

# Force test mode (set to false for production)
REACT_APP_FORCE_TEST_MODE=false
